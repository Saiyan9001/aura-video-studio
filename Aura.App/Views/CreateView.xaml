<Page
    x:Class="Aura.App.Views.CreateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Aura.App.Views"
    xmlns:viewmodels="using:Aura.App.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="32" MaxWidth="1200" Spacing="20">
            
            <!-- Header -->
            <StackPanel Spacing="8" Margin="0,0,0,16">
                <TextBlock 
                    Text="Create New Video" 
                    Style="{ThemeResource TitleTextBlockStyle}"/>
                <TextBlock 
                    Text="Configure your video content, style, and narration preferences"
                    Style="{ThemeResource BodyTextBlockStyle}"
                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
            </StackPanel>
            
            <!-- Step 1: Brief -->
            <Expander Header="Step 1: Brief" IsExpanded="True">
                <StackPanel Spacing="16" Padding="16">
                    <TextBox 
                        Header="Topic (Required)"
                        PlaceholderText="e.g., 'Introduction to Machine Learning' or 'Best Hiking Trails in Colorado'"
                        Text="{x:Bind ViewModel.Topic, Mode=TwoWay}"
                        MaxLength="200"
                        ToolTipService.ToolTip="The main subject or title of your video"/>
                    
                    <TextBox 
                        Header="Audience (Optional)"
                        PlaceholderText="e.g., 'Beginners', 'Professionals', 'Students'"
                        Text="{x:Bind ViewModel.Audience, Mode=TwoWay}"
                        ToolTipService.ToolTip="Who is this video intended for?"/>
                    
                    <TextBox 
                        Header="Goal (Optional)"
                        PlaceholderText="e.g., 'Learn the basics' or 'Discover new places'"
                        Text="{x:Bind ViewModel.Goal, Mode=TwoWay}"
                        ToolTipService.ToolTip="What should viewers achieve after watching?"/>
                    
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <ComboBox 
                            Grid.Column="0"
                            Header="Tone"
                            SelectedItem="{x:Bind ViewModel.Tone, Mode=TwoWay}"
                            HorizontalAlignment="Stretch"
                            ToolTipService.ToolTip="The style and personality of your video">
                            <x:String>Informative</x:String>
                            <x:String>Casual</x:String>
                            <x:String>Professional</x:String>
                            <x:String>Entertaining</x:String>
                            <x:String>Educational</x:String>
                        </ComboBox>
                        
                        <ComboBox 
                            Grid.Column="1"
                            Header="Language"
                            SelectedItem="{x:Bind ViewModel.Language, Mode=TwoWay}"
                            HorizontalAlignment="Stretch"
                            ToolTipService.ToolTip="Narration language">
                            <x:String>en-US</x:String>
                            <x:String>en-GB</x:String>
                            <x:String>es-ES</x:String>
                            <x:String>fr-FR</x:String>
                            <x:String>de-DE</x:String>
                        </ComboBox>
                    </Grid>
                    
                    <ComboBox 
                        Header="Aspect Ratio"
                        Width="300"
                        HorizontalAlignment="Left"
                        SelectedIndex="0"
                        ToolTipService.ToolTip="Video dimensions for your target platform">
                        <ComboBoxItem Content="16:9 Widescreen" Tag="Widescreen16x9"
                                       ToolTipService.ToolTip="Common for YouTube, Desktop"/>
                        <ComboBoxItem Content="9:16 Vertical" Tag="Vertical9x16"
                                       ToolTipService.ToolTip="Common for Shorts, TikTok, Reels"/>
                        <ComboBoxItem Content="1:1 Square" Tag="Square1x1"
                                       ToolTipService.ToolTip="Common for Instagram Feed"/>
                    </ComboBox>
                </StackPanel>
            </Expander>
            
            <!-- Step 2: Length and Pacing -->
            <Expander Header="Step 2: Length and Pacing" IsExpanded="True">
                <StackPanel Spacing="16" Padding="16">
                    <StackPanel Spacing="8">
                        <TextBlock 
                            Text="{x:Bind ViewModel.DurationMinutes, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='Duration: {0} minutes'}"
                            Style="{ThemeResource SubtitleTextBlockStyle}"/>
                        <Slider 
                            Minimum="1" 
                            Maximum="20" 
                            Value="{x:Bind ViewModel.DurationMinutes, Mode=TwoWay}"
                            StepFrequency="1"
                            TickFrequency="1"
                            TickPlacement="BottomRight"
                            ToolTipService.ToolTip="Target video length (actual duration may vary)"/>
                        <TextBlock 
                            Text="Adjust the target length for your video content"
                            Style="{ThemeResource CaptionTextBlockStyle}"
                            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                    </StackPanel>
                    
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <ComboBox 
                            Grid.Column="0"
                            Header="Pacing"
                            HorizontalAlignment="Stretch"
                            SelectedIndex="1"
                            ToolTipService.ToolTip="Speaking speed: Chill ≈ 140 wpm, Conversational ≈ 160 wpm, Fast ≈ 190 wpm">
                            <ComboBoxItem Content="Chill (Relaxed)" Tag="Chill"/>
                            <ComboBoxItem Content="Conversational (Natural)" Tag="Conversational"/>
                            <ComboBoxItem Content="Fast (Energetic)" Tag="Fast"/>
                        </ComboBox>
                        
                        <ComboBox 
                            Grid.Column="1"
                            Header="Content Density"
                            HorizontalAlignment="Stretch"
                            SelectedIndex="1"
                            ToolTipService.ToolTip="How much information to pack into the video">
                            <ComboBoxItem Content="Minimal (Focused)" Tag="Minimal"/>
                            <ComboBoxItem Content="Balanced (Recommended)" Tag="Balanced"/>
                            <ComboBoxItem Content="Dense (Comprehensive)" Tag="Dense"/>
                        </ComboBox>
                    </Grid>
                    
                    <TextBox 
                        Header="Style"
                        Text="{x:Bind ViewModel.Style, Mode=TwoWay}"
                        PlaceholderText="e.g., Educational, Tutorial, Review, Documentary"
                        ToolTipService.ToolTip="Overall presentation style (optional)"/>
                </StackPanel>
            </Expander>
            
            <!-- Step 3: Voice -->
            <Expander Header="Step 3: Voice and Narration" IsExpanded="True">
                <StackPanel Spacing="16" Padding="16">
                    <ComboBox 
                        Header="Voice"
                        SelectedItem="{x:Bind ViewModel.VoiceName, Mode=TwoWay}"
                        HorizontalAlignment="Stretch"
                        MaxWidth="500"
                        ToolTipService.ToolTip="Select from Windows built-in voices (free) or add API keys in Settings for premium voices">
                        <x:String>Microsoft David Desktop</x:String>
                        <x:String>Microsoft Zira Desktop</x:String>
                        <x:String>Microsoft Mark</x:String>
                    </ComboBox>
                    
                    <StackPanel Spacing="8">
                        <TextBlock 
                            Text="{x:Bind ViewModel.VoiceRate, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='Speech Rate: {0:F1}x'}"
                            Style="{ThemeResource SubtitleTextBlockStyle}"/>
                        <Slider 
                            Minimum="0.5" 
                            Maximum="2.0" 
                            Value="{x:Bind ViewModel.VoiceRate, Mode=TwoWay}"
                            StepFrequency="0.1"
                            TickFrequency="0.25"
                            TickPlacement="BottomRight"
                            ToolTipService.ToolTip="1.0x is normal speed, 1.5x is faster, 0.75x is slower"/>
                        <TextBlock 
                            Text="0.5x (Slow) — 1.0x (Normal) — 2.0x (Fast)"
                            Style="{ThemeResource CaptionTextBlockStyle}"
                            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                            HorizontalAlignment="Center"/>
                    </StackPanel>
                    
                    <StackPanel Spacing="8">
                        <TextBlock 
                            Text="{x:Bind ViewModel.VoicePitch, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='Pitch: {0:F1}'}"
                            Style="{ThemeResource SubtitleTextBlockStyle}"/>
                        <Slider 
                            Minimum="-10" 
                            Maximum="10" 
                            Value="{x:Bind ViewModel.VoicePitch, Mode=TwoWay}"
                            StepFrequency="1"
                            TickFrequency="5"
                            TickPlacement="BottomRight"
                            ToolTipService.ToolTip="Adjust voice pitch: negative values for deeper, positive for higher"/>
                        <TextBlock 
                            Text="-10 (Deeper) — 0 (Normal) — +10 (Higher)"
                            Style="{ThemeResource CaptionTextBlockStyle}"
                            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                            HorizontalAlignment="Center"/>
                    </StackPanel>
                    
                    <ComboBox 
                        Header="Pause Style"
                        Width="300"
                        HorizontalAlignment="Left"
                        SelectedIndex="1"
                        ToolTipService.ToolTip="Duration of pauses between sentences">
                        <ComboBoxItem Content="Short (Quick pace)" Tag="Short"/>
                        <ComboBoxItem Content="Natural (Recommended)" Tag="Natural"/>
                        <ComboBoxItem Content="Long (More breathing room)" Tag="Long"/>
                    </ComboBox>
                </StackPanel>
            </Expander>
            
            <!-- Step 4: Visuals -->
            <Expander Header="Step 4: Visuals" IsExpanded="True">
                <StackPanel Spacing="16" Padding="16">
                    <ComboBox 
                        Header="Visual Mode"
                        SelectedItem="{x:Bind ViewModel.VisualMode, Mode=TwoWay}"
                        HorizontalAlignment="Stretch"
                        MaxWidth="500"
                        ToolTipService.ToolTip="Choose visual generation strategy">
                        <x:String>Stock</x:String>
                        <x:String>StockOrLocal</x:String>
                        <x:String>Pro</x:String>
                    </ComboBox>
                    
                    <TextBlock 
                        Text="Stock Provider Weights (must sum to 100%)"
                        Style="{ThemeResource SubtitleTextBlockStyle}"/>
                    
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Spacing="8">
                            <TextBlock 
                                Text="{x:Bind ViewModel.PexelsWeight, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='Pexels: {0}%'}"
                                Style="{ThemeResource CaptionTextBlockStyle}"/>
                            <Slider 
                                Minimum="0" 
                                Maximum="100" 
                                Value="{x:Bind ViewModel.PexelsWeight, Mode=TwoWay}"
                                StepFrequency="1"
                                ToolTipService.ToolTip="Weight for Pexels stock images"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Spacing="8">
                            <TextBlock 
                                Text="{x:Bind ViewModel.PixabayWeight, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='Pixabay: {0}%'}"
                                Style="{ThemeResource CaptionTextBlockStyle}"/>
                            <Slider 
                                Minimum="0" 
                                Maximum="100" 
                                Value="{x:Bind ViewModel.PixabayWeight, Mode=TwoWay}"
                                StepFrequency="1"
                                ToolTipService.ToolTip="Weight for Pixabay stock images"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2" Spacing="8">
                            <TextBlock 
                                Text="{x:Bind ViewModel.UnsplashWeight, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='Unsplash: {0}%'}"
                                Style="{ThemeResource CaptionTextBlockStyle}"/>
                            <Slider 
                                Minimum="0" 
                                Maximum="100" 
                                Value="{x:Bind ViewModel.UnsplashWeight, Mode=TwoWay}"
                                StepFrequency="1"
                                ToolTipService.ToolTip="Weight for Unsplash stock images"/>
                        </StackPanel>
                    </Grid>
                    
                    <TextBlock 
                        Text="Stable Diffusion Parameters (StockOrLocal mode only)"
                        Style="{ThemeResource SubtitleTextBlockStyle}"
                        Margin="0,8,0,0"/>
                    
                    <InfoBar
                        Title="NVIDIA GPU Required"
                        Message="Local Stable Diffusion requires an NVIDIA GPU with at least 6GB VRAM. SDXL model requires 12GB+."
                        Severity="Informational"
                        IsClosable="False"/>
                    
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <ComboBox 
                            Grid.Column="0"
                            Header="Model"
                            SelectedItem="{x:Bind ViewModel.SdModel, Mode=TwoWay}"
                            HorizontalAlignment="Stretch"
                            ToolTipService.ToolTip="Auto selects based on VRAM (SDXL for 12GB+, SD1.5 for 6-12GB)">
                            <x:String>Auto</x:String>
                            <x:String>SDXL</x:String>
                            <x:String>SD15</x:String>
                        </ComboBox>
                        
                        <ComboBox 
                            Grid.Column="1"
                            Header="Size"
                            SelectedItem="{x:Bind ViewModel.SdSize, Mode=TwoWay}"
                            HorizontalAlignment="Stretch"
                            ToolTipService.ToolTip="Image dimensions (Auto matches aspect ratio)">
                            <x:String>Auto</x:String>
                            <x:String>1024x576</x:String>
                            <x:String>576x1024</x:String>
                            <x:String>1024x1024</x:String>
                        </ComboBox>
                    </Grid>
                    
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Spacing="8">
                            <TextBlock 
                                Text="{x:Bind ViewModel.SdSteps, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='Steps: {0}'}"
                                Style="{ThemeResource CaptionTextBlockStyle}"/>
                            <Slider 
                                Minimum="5" 
                                Maximum="50" 
                                Value="{x:Bind ViewModel.SdSteps, Mode=TwoWay}"
                                StepFrequency="5"
                                TickFrequency="10"
                                TickPlacement="BottomRight"
                                ToolTipService.ToolTip="Quality vs speed (20-30 recommended)"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Spacing="8">
                            <TextBlock 
                                Text="{x:Bind ViewModel.SdCfgScale, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='CFG Scale: {0:F1}'}"
                                Style="{ThemeResource CaptionTextBlockStyle}"/>
                            <Slider 
                                Minimum="1" 
                                Maximum="20" 
                                Value="{x:Bind ViewModel.SdCfgScale, Mode=TwoWay}"
                                StepFrequency="0.5"
                                TickFrequency="5"
                                TickPlacement="BottomRight"
                                ToolTipService.ToolTip="Prompt adherence (7-9 recommended)"/>
                        </StackPanel>
                    </Grid>
                    
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox 
                            Grid.Column="0"
                            Header="Seed"
                            Text="{x:Bind ViewModel.SdSeed, Mode=TwoWay}"
                            PlaceholderText="-1 for random"
                            ToolTipService.ToolTip="Set for reproducible results, -1 for random"/>
                        
                        <ComboBox 
                            Grid.Column="1"
                            Header="Style"
                            SelectedItem="{x:Bind ViewModel.VisualStyle, Mode=TwoWay}"
                            HorizontalAlignment="Stretch"
                            ToolTipService.ToolTip="Visual style for generated images">
                            <x:String>cinematic</x:String>
                            <x:String>photographic</x:String>
                            <x:String>artistic</x:String>
                            <x:String>abstract</x:String>
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Expander>
            
            <!-- Progress and Actions -->
            <StackPanel Spacing="16" Margin="0,8,0,0">
                <ProgressBar 
                    Value="{x:Bind ViewModel.ProgressPercentage, Mode=OneWay}"
                    Visibility="{x:Bind ViewModel.IsGenerating, Mode=OneWay}"
                    ShowError="False"
                    ShowPaused="False"
                    Height="8"/>
                
                <TextBlock 
                    Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                    Style="{ThemeResource BodyTextBlockStyle}"
                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                    TextWrapping="Wrap"/>
                
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button 
                        Content="Generate Video" 
                        Command="{x:Bind ViewModel.GenerateVideoCommand}"
                        IsEnabled="{x:Bind ViewModel.IsGenerating, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"
                        Style="{ThemeResource AccentButtonStyle}"
                        Padding="24,8"
                        ToolTipService.ToolTip="Start creating your video with the configured settings"/>
                    
                    <Button 
                        Content="Reset Form" 
                        Command="{x:Bind ViewModel.ResetFormCommand}"
                        IsEnabled="{x:Bind ViewModel.IsGenerating, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"
                        Padding="24,8"
                        ToolTipService.ToolTip="Clear all fields and start fresh"/>
                </StackPanel>
            </StackPanel>
            
            <!-- Info Card -->
            <InfoBar
                Title="Free Mode Active"
                Message="Using rule-based script generation and Windows TTS. Add API keys in Settings for Pro features like GPT-4, ElevenLabs voices, and more."
                IsOpen="True"
                Severity="Informational"
                IsClosable="False"
                Margin="0,8,0,0"/>
        </StackPanel>
    </ScrollViewer>
</Page>
